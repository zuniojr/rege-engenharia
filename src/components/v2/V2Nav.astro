---
---

<nav class="v2-nav" id="v2-nav">
  <div class="v2-nav-inner">
    <a href="/" class="v2-logo">
      <img src="/images/logo.png" alt="Regê Engenharia" class="v2-logo-img" />
    </a>
    <ul class="v2-nav-links">
      <li><a href="/sobre">Sobre</a></li>
      <li><a href="/servicos">Serviços</a></li>
      <li><a href="/portfolio">Portfólio</a></li>
      <li><a href="/blog">BLOG</a></li>
    </ul>
    <a href="https://bitlybr.net/rege" class="v2-nav-cta" target="_blank" rel="noopener noreferrer">
      Fale Conosco
    </a>
    <button class="v2-hamburger" id="v2-hamburger" aria-label="Menu">
      <span></span><span></span><span></span>
    </button>
  </div>
  <div class="v2-mobile-menu" id="v2-mobile-menu">
    <a href="/sobre">Sobre</a>
    <a href="/servicos">Serviços</a>
    <a href="/portfolio">Portfólio</a>
    <a href="/blog">BLOG</a>
    <a href="https://bitlybr.net/rege" class="v2-mobile-cta" target="_blank">Fale Conosco</a>
  </div>
</nav>

<style>
/* NAV */
.v2-nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  transition: background 0.4s var(--v2-ease), box-shadow 0.4s var(--v2-ease);
}
.v2-nav.scrolled {
  background: rgba(8,14,23,0.92);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  box-shadow: 0 1px 0 var(--v2-border);
}
.v2-nav-inner {
  display: flex;
  align-items: center;
  gap: 2.5rem;
  max-width: 1200px;
  margin: 0 auto;
  padding: 1.4rem 1.5rem;
}
.v2-logo {
  display: flex;
  align-items: center;
  text-decoration: none;
  flex-shrink: 0;
}
.v2-logo-img {
  height: 40px;
  width: auto;
  display: block;
}
.v2-nav-links {
  display: flex;
  list-style: none;
  gap: 2rem;
  margin: 0 0 0 auto;
  padding: 0;
}
.v2-nav-links a {
  text-decoration: none;
  font-family: var(--v2-font-display);
  font-size: 0.7rem;
  font-weight: 700;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  color: rgba(255,255,255,0.7);
  transition: color 0.25s;
}
.v2-nav-links a:hover { color: var(--v2-gold); }
.v2-nav-cta {
  font-family: var(--v2-font-display);
  font-size: 0.68rem;
  font-weight: 700;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  color: var(--v2-dark);
  background: var(--v2-gold);
  padding: 0.6rem 1.4rem;
  border-radius: 3px;
  text-decoration: none;
  transition: background 0.25s, transform 0.2s;
  flex-shrink: 0;
}
.v2-nav-cta:hover { background: var(--v2-gold-2); transform: translateY(-1px); }
.v2-hamburger {
  display: none;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 5px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 8px;
  min-width: 44px;
  min-height: 44px;
  margin-left: auto;
}
.v2-hamburger span {
  display: block;
  width: 22px;
  height: 2px;
  background: var(--v2-white);
  transition: all 0.3s;
}
.v2-mobile-menu {
  display: none;
  flex-direction: column;
  background: rgba(8,14,23,0.98);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-top: 1px solid var(--v2-border);
  box-shadow: 0 20px 40px rgba(0,0,0,0.5);
  padding: 0.5rem 0;
}
.v2-mobile-menu a {
  text-decoration: none;
  font-family: var(--v2-font-display);
  font-size: 0.78rem;
  font-weight: 700;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: rgba(255,255,255,0.85);
  padding: 1rem 1.5rem;
  min-height: 48px;
  display: flex;
  align-items: center;
  border-bottom: 1px solid var(--v2-border);
  transition: color 0.2s, background 0.2s;
}
.v2-mobile-menu a:hover,
.v2-mobile-menu a:active { color: var(--v2-gold); background: rgba(201,168,76,0.08); }
.v2-mobile-cta { 
  margin: 1rem 1.25rem 0.75rem !important;
  background: var(--v2-gold) !important;
  color: var(--v2-dark) !important;
  border-radius: var(--v2-radius) !important;
  justify-content: center !important;
  min-height: 52px !important;
  border-bottom: none !important;
  box-shadow: 0 4px 15px rgba(201,168,76,0.3);
}
.v2-mobile-menu.open { display: flex; }

@media (max-width: 900px) {
  .v2-nav-links { display: none; }
  .v2-nav-cta { display: none; }
  .v2-hamburger { display: flex; }
  .v2-nav-inner { gap: 1rem; padding: 0.75rem 1.25rem; }
  .v2-logo-img { height: 25px; }
}
</style>

<script>
  // NAV scroll
  const nav = document.getElementById('v2-nav');
  window.addEventListener('scroll', () => {
    nav?.classList.toggle('scrolled', window.scrollY > 60);
  }, { passive: true });

  // Hamburger
  const hamburger = document.getElementById('v2-hamburger');
  const mobileMenu = document.getElementById('v2-mobile-menu');
  hamburger?.addEventListener('click', () => mobileMenu?.classList.toggle('open'));
  mobileMenu?.querySelectorAll('a').forEach(a => {
    a.addEventListener('click', () => mobileMenu.classList.remove('open'));
  });

  // Smooth scroll
  document.querySelectorAll('a[href^="#v2-"]').forEach(anchor => {
    anchor.addEventListener('click', (e) => {
      e.preventDefault();
      const target = document.querySelector((anchor as HTMLAnchorElement).getAttribute('href')!);
      target?.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });
  });
</script>
